image: docker:latest
services:
  - docker:dind

stages:
  - build
  
build:
  stage: build
  script:
    - docker build -t <dockerhub-username>/my-app:latest .
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD"
    - docker push <dockerhub-username>/my-app:latest


