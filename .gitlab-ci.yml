image: docker:latest
services:
  - docker:dind

stages:
  - build

build:
  stage: build
  script:
    - docker build -t kaneki100/teste0:latest .
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD"
    - docker push kaneki100/teste0:latest


